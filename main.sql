SELECT "Hello world!";

create table personas (
  id_persona   integer primary key,
  nombre varchar(30) no null,
  primer_apellido varchar(30) no null,
  segundo_apellido  vaechar(30) no null,
  email varchar(30) no null);